# =============================================================================
# AEROFINDER Backend — Dependencias Python
# Versiones fijadas para builds reproducibles
# =============================================================================

# Framework web async
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Base de datos
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6
geoalchemy2==0.15.2

# Autenticación JWT
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Validación y configuración
pydantic[email]==2.10.3
pydantic-settings==2.7.0
python-multipart==0.0.20

# Procesamiento de imágenes (análisis de fotos para validación y atributos)
Pillow>=10.0.0
opencv-python-headless>=4.9.0
numpy>=1.26.0

# Redis Streams
redis[hiredis]==5.2.1

# MinIO / almacenamiento S3-compatible
minio==7.2.12

# HTTP client (para WebSockets salientes y llamadas internas)
httpx==0.28.1
websockets==14.1

# Utilidades
python-dateutil==2.9.0
shapely==2.0.6

# Notificaciones Push PWA
pywebpush==2.0.0

# OCR de documentos de identidad (CI boliviana, pasaporte)
paddlepaddle-cpu==2.6.1
paddleocr==2.7.3
